{% extends 'base.html.twig' %}

{% block title %}Edit Caategory{% endblock %}

{% block body %}
    <h1>Edit Caategory</h1>

    {{ include('caategory/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('caategory_index') }}">back to list</a>

    {{ include('caategory/_delete_form.html.twig') }}
{% endblock %}
